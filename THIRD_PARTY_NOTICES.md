# Third-party notices

DeepTradeAlgo depends on open-source Python packages listed in `pyproject.toml` and locked in `uv.lock`. Their package metadata and upstream repositories contain the controlling license terms.

Market-data access is provided through the third-party `yfinance` package and Yahoo Finance endpoints. Yahoo Finance is not affiliated with this project. Availability, delay, and permitted use are governed by the applicable upstream terms. When live data is unavailable, the app labels its deterministic generated data as demo data.

The generated `static/vendor/plotly.min.js` file is produced from the locked Plotly Python package. Plotly.js is distributed under the MIT License; its upstream copyright and license header are preserved in the generated asset.
